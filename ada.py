def ada():
    first_name = "AdA"
    last_name = "LoVeLAce"

    format1 = f"{first_name.lower()} {last_name.lower()}"
    format2 = f"{first_name.capitalize()} {last_name.capitalize()}"
    format3 = f"{first_name.upper()} {last_name.upper()}"
    format4 = f"\t{first_name.lower()} {last_name.lower()}"

    print(format1)
    print(format2)
    print(format3)
    print(format4)

# Call the function to execute it
ada()

